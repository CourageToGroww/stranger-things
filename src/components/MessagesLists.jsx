//A component to display all messages received by the authenticated user.
const MessagesLists = () => {
  return (
    <div>
      <h1>Message Lists</h1>
    </div>
  );
};

export default MessagesLists;
