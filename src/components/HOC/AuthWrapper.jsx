//wrapper to manage authentication-related logic, ie: token handling and conditional rendering based on authentication status
const AuthWrapper = () => {
  return (
    <div>
      <h1> Auth Wrapper</h1>
    </div>
  );
};

export default AuthWrapper;
