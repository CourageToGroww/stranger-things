//a button component to delete posts, visible only to the author of the post

const DeletePostButton = () => {
  return (
    <div>
      <h1>DeletePostButton</h1>
    </div>
  );
};

export default DeletePostButton;
